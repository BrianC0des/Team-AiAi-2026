---
description: Tells the browser agent to check the full stack health.
---

1. Run `npm run dev` in the background using the Terminal Agent.
2. Launch the integrated Browser Agent and navigate to `http://localhost:3000`.
3. Verify that the vanilla HTML frontend accurately loads and communicates with the Express backend.
4. Capture a screenshot artifact if any API calls fail.